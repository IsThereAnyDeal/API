$schema: https://json-schema.org/draft/2020-12/schema
type: object
required: [url, events]
properties:
  url:
    type: string
    format: uri
  events:
    type: array
    minItems: 1
    items:
      type: string
      enum: [notification-waitlist]
